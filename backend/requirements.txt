# ---------------------------------------------------------------------------
# sulfur-news backend 运行依赖
#
# 版本采用 ">=下限,<主版本上限" 策略：
#   - 保证拿到安全更新
#   - 避免主版本破坏性变更（尤其 pydantic v2、fastapi 0.x）
# ---------------------------------------------------------------------------

# --- Web 框架 ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- 数据模型 / 配置 ---
pydantic>=2.6,<3.0
python-dotenv>=1.0,<2.0

# --- 抓取 ---
httpx>=0.27,<1.0
feedparser>=6.0,<7.0

# ---------------------------------------------------------------------------
# 可选：接入自定义网页解析器时再启用
# ---------------------------------------------------------------------------
# beautifulsoup4>=4.12,<5.0
# lxml>=5.0,<6.0

# ---------------------------------------------------------------------------
# 开发依赖（如使用 pip-tools / pipenv 可拆到 requirements-dev.txt）
# ---------------------------------------------------------------------------
# pytest>=8.0
# pytest-asyncio>=0.23
# ruff>=0.4
# mypy>=1.9
